import 'package:flutter/material.dart';

class AddOrEditEventScreen extends StatelessWidget {
  const AddOrEditEventScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
